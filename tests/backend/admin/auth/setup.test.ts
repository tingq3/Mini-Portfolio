/**
 * Tests for POST /api/admin/auth/setup
 *
 * Allows users to set up authentication on first-run.
 */

it.todo('Blocks all access after first call');

it.todo('Refuses to accept weak passwords');

it.todo('Stores the username and a (hashed and salted) password in data/config.local.json');
