/**
 * Tests for POST /api/admin/auth/change
 *
 * Allows users to change the auth info, provided they are logged in.
 */

it.todo('Blocks unauthorized users');

it.todo('Allows users to reset their password');

it.todo('Rejects non-existent usernames');

it.todo('Rejects incorrect previous passwords');

it.todo('Rejects insecure new passwords');
