/**
 * Tests for POST /api/admin/auth/login
 *
 * Allows users to log into the site, enabling editing of the data.
 */

it.todo('Returns a token when correct credentials are provided');

it.todo('Blocks logins with non-existent usernames');

it.todo('Blocks logins with incorrect passwords');
