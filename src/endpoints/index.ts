/** API endpoints */
import admin from './admin';
import debug from './debug';
import group from './group';
import readme from './readme';

const api = {
  admin,
  debug,
  group,
  readme,
};

export default api;
